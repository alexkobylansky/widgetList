import React from "react";

export default function GoToCabinetButton({icon}) {
  return (
    //ToDo: Add React Router function to go to personal cabinet
    <button type={"button"} className={"btn main-btn"} onClick={() => {}}><span className="icon">{icon}</span>Go To Cabinet</button>
  )

}